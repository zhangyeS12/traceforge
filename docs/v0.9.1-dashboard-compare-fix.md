# TraceForge v0.9.1 — Dashboard Compare Dropdown Fix

This patch fixes a Dashboard UI bug where the Compare dropdowns were rendered but not populated after the run list loaded.

## Changes

- Calls `renderCompareOptions()` after `/api/runs` data is loaded.
- Disables Compare controls when there are fewer than two runs.
- Adds clearer helper text for empty and one-run states.

