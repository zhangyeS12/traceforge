# TraceForge v1.1.1 — Reindex Local Runs

This patch adds a recovery command for local workspaces:

```powershell
traceforge reindex
```

It scans `.traceforge/runs` and `.traceforge/reports`, then rebuilds missing SQLite rows for runs whose artifact folders still exist.

Use this when the Dashboard shows fewer runs than the artifact folders on disk.
